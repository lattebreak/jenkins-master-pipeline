pipeline {
    agent {
        label 'jenkins-agent1'
    }
    stages {
        stage('Build Jenkins Image') {
            steps {
                cat './jenkins-dockerfile/dockerrun.sh'
            }
        }
    }
}