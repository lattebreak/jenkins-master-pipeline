pipeline {
    agent 'docker agent'
    stages {
        stage('Build Jenkins Image') {
            steps {
                cd jenkins-pipeline
                echo $pwd
                echo "This is a test"
            }
        }
    }
}