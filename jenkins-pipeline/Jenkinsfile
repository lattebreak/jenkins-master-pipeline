pipeline {
    agent {
        label 'jenkins-agent1'
    }
    stages {
        stage('Build Jenkins Image') {
            steps {
                sh 'python3 test_build.py'
            }
        }
    }
}