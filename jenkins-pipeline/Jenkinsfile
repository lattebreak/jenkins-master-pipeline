pipeline {
    agent {
        label 'jenkins-agent1'
    }
    stages {
        stage('Build Docker agent Image') {
            steps {
                echo 'hello hi'
                // sh 'service docker start'
                // sh 'service docker status'
                // sh 'docker version'
            }
        }
    }
}