pipeline {
    agent {
        label 'jenkins-agent1'
    }
    environment {
        DOCKER_HOST='tcp://172.17.0.1:2375'
    }
    stages {
        stage('Build Docker agent Image') {
            steps {
                sh 'docker version'
            }
        }
    }
}