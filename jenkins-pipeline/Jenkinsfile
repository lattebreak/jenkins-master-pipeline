pipeline {
    agent {
        label 'jenkins-agent1'
    }
    stages {
        stage('Build Docker agent Image') {
            steps {
                sh 'docker run -v /var/run/docker.sock:/var/run/docker.sock -it jenkins/agent:latest-alpine'
                sh 'docker version'
            }
        }
    }
}